## Summary
<!-- What and why -->

## Checklist
- [ ] New cameras return `image/jpeg` or `image/png` on plain HTTP GET (sample curls in description)
- [ ] No private / auth-walled / non-public feeds
- [ ] Schema fields present: id, name, url, country, city, location, timezone, coordinates, category, verified, auth
- [ ] `source_id` set when adding a bulk source (see `sources/catalog.json`)
- [ ] `npm test` passes for code changes
- [ ] No secrets committed

## Test plan
- [ ] …
